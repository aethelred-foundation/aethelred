name: Security Scans

on:
  push:
    branches: [main, develop, "release/**"]
  pull_request:
    branches: [main, develop, "release/**"]
  workflow_dispatch:

env:
  GO_VERSION: "1.25.8"
  RUST_VERSION: "1.85.0"
  GOFLAGS: "-mod=mod"
  STRICT_SECURITY_GATES: "false"  # Relaxed until upstream dependency vulnerabilities are resolved

jobs:
  solidity-static-analysis:
    name: Solidity Static Analysis
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: contracts
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install contract dependencies
        run: npm install --no-fund --no-audit
      - name: Slither scan
        continue-on-error: ${{ env.STRICT_SECURITY_GATES != 'true' }}
        uses: crytic/slither-action@v0.4.0
        with:
          target: contracts
          slither-config: contracts/slither.config.json

  dependency-vuln-scan:
    name: Dependency Vulnerability Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Go vulnerability scan
        continue-on-error: ${{ env.STRICT_SECURITY_GATES != 'true' }}
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked
      - name: Rust advisory audit
        continue-on-error: ${{ env.STRICT_SECURITY_GATES != 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          lockfiles=(
            "crates/Cargo.lock"
            "sdk/rust/Cargo.lock"
            "sdk/aethelred-sdk/Cargo.lock"
            "sdk/aethelred-py/Cargo.lock"
          )
          for lockfile in "${lockfiles[@]}"; do
            echo ">>> cargo audit --file ${lockfile}"
            cargo audit --file "${lockfile}"
          done

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: NPM dependency audit (contracts)
        continue-on-error: ${{ env.STRICT_SECURITY_GATES != 'true' }}
        working-directory: contracts
        run: |
          npm install --no-fund --no-audit
          npm audit --omit=dev --audit-level=high

  sast-and-secrets:
    name: SAST + Secrets Scan
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Run gosec
        continue-on-error: ${{ env.STRICT_SECURITY_GATES != 'true' }}
        uses: securego/gosec@master
        with:
          # G104: unchecked errors (handled by errcheck linter in CI)
          # G115: integer overflow conversions — high false-positive rate on Cosmos SDK
          #       chains where proto int64 ↔ Go uint64 casts are ubiquitous and safe
          #       in context (block heights are always positive, byte encoding is intentional)
          args: -severity medium -confidence medium -exclude=G104,G115 ./...

      - name: Trivy filesystem scan
        continue-on-error: ${{ env.STRICT_SECURITY_GATES != 'true' }}
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # v0.35.0 pinned by SHA
        with:
          scan-type: fs
          scan-ref: "."
          scanners: "vuln"
          format: sarif
          output: trivy-results.sarif
          severity: "HIGH,CRITICAL"
          exit-code: "1"

      - name: Upload Trivy SARIF
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-results.sarif

      - name: Gitleaks scan
        continue-on-error: ${{ env.STRICT_SECURITY_GATES != 'true' }}
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

  security-required-gate:
    name: Security Required Gate
    if: ${{ always() && github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    needs:
      - solidity-static-analysis
      - dependency-vuln-scan
      - sast-and-secrets
    steps:
      - name: Enforce required gate status
        run: |
          echo "solidity-static-analysis=${{ needs.solidity-static-analysis.result }}"
          echo "dependency-vuln-scan=${{ needs.dependency-vuln-scan.result }}"
          echo "sast-and-secrets=${{ needs.sast-and-secrets.result }}"
          test "${{ needs.sast-and-secrets.result }}" = "success"
