version: v2

modules:
  - path: proto
  - path: third_party/proto

lint:
  use:
    - STANDARD
  except:
    - IMPORT_USED
    - SERVICE_SUFFIX
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
  ignore:
    - third_party/proto

breaking:
  use:
    - FILE
