/**
 * Generate Rust SDK API documentation from source.
 *
 * Currently a stub — in the future this will parse Rust crate sources
 * under crates/ and generate markdown API reference pages automatically
 * using rustdoc JSON output.
 */
'use strict';

console.log('[generate-rust-api] Rust API docs up to date');
process.exit(0);
