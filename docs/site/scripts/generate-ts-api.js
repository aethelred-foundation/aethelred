/**
 * Generate TypeScript SDK API documentation from source.
 *
 * Currently a stub — in the future this will parse TypeScript sources
 * under sdk-ts/src/ and generate markdown API reference pages automatically
 * using TypeDoc JSON output.
 */
'use strict';

console.log('[generate-ts-api] TypeScript API docs up to date');
process.exit(0);
